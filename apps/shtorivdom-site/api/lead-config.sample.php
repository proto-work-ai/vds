<?php
/**
 * Образец настроек приёма заявок.
 *
 * На хостинге скопируйте этот файл рядом под именем lead-config.php и впишите свои значения.
 * lead-config.php в репозиторий не попадает (см. .gitignore) — пароль остаётся только на сервере.
 */

return [
    // Кому уходят письма о заявках
    'to' => ['info@shtorivdom.ru', 'andrey.asp2010@gmail.com'],

    // Ящик домена, от имени которого отправляются письма.
    // Timeweb: сервер smtp.timeweb.ru, порты 465 (ssl), 587 (tls), 25 или 2525 без шифрования.
    // Логин — сам адрес ящика. Адрес отправителя (from) обязан совпадать с логином:
    // подмена отправителя на Timeweb запрещена, письмо просто не уйдёт.
    'smtp' => [
        'host' => 'smtp.timeweb.ru',
        'port' => 465,
        'secure' => 'ssl',            // ssl для порта 465, tls для 587
        'user' => 'info@shtorivdom.ru',
        'password' => 'ПАРОЛЬ_ЯЩИКА', // вписать на сервере, в репозиторий не коммитить
        'from' => 'info@shtorivdom.ru',
        'fromName' => 'Шторы в дом',
        // Адрес для заголовка List-Unsubscribe (кнопка «Отписаться» в почте).
        // Для писем о заявках не обязателен, но повышает доверие спам-фильтров. Пусто — заголовка не будет.
        'unsubscribe' => 'info@shtorivdom.ru',
        'timeout' => 20,
    ],

    // Дубль заявки в Telegram. Оставьте пустым, если не нужно.
    // token — от @BotFather, chatId — свой id можно узнать у @userinfobot,
    // для группы добавьте бота в неё и возьмите id группы (начинается с минуса).
    'telegram' => [
        'token' => '',
        'chatId' => '',
    ],

    // С каких доменов принимаем заявки
    'allowedHosts' => ['shtorivdom.ru', 'www.shtorivdom.ru'],

    // Не больше стольких заявок с одного адреса за 10 минут
    'rateLimit' => 5,
];
