# Шаблон внутренней страницы

- `_stub.html` не создаёт отдельный маршрут.
- При использовании placeholder-данные заменяются содержимым из `site-pages.ts`.
- Новый HTML-файл сразу получает одноимённый `.angular.md` по `_template.angular.md`.
